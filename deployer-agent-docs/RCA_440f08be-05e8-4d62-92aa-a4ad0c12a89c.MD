# RCA (CORRECTED) — Rahaal ERP Smart Reader read failure — SCOPING MISMATCH
Follow-up with owner's live reproduction (~06:56 UTC). Evidence-only. NO deploy / mutation / migration.

## HEADLINE — the failing read did NOT occur on the deployment this agent is scoped to
I am scoped to deployment **`visa-booking-5`** (`deployment_id 660386d0-cfd0-4f42-bf1b-6e8adf188919`), served at **`https://visa-booking-5.emergent.host`**, active run **`440f08be`** (image `:440f08be`, built 2026-09-17). Three independent facts prove the owner's reproduced read on `targetmediagrp.com` did NOT hit this deployment:

1. **No custom domain here.** `list_domains` and `check_domain_status` both return an **empty list**. `targetmediagrp.com` is NOT attached to this deployment. This deployment answers only on `visa-booking-5.emergent.host`.
2. **No Smart Reader data in this DB.** DB bound by the running container = `visa-booking-5-your_database_name`. `smart_reader_usage` → **collection_not_found**; `smart_reader_config` / `smart_reader_credits` are **not among the 22 collections**. The owner's "«10 قراءة متبقية»" banner requires a successful `GET /credits` that lazily seeds `smart_reader_config` + `smart_reader_credits` — those collections do not exist here, so that call never succeeded against this DB.
3. **No Smart Reader traffic in this pod's logs.** 24h window (max) filtered for `smart-reader|smartReader|EMERGENT|emergentintegrations|pdfjs|pdf|LLM|gemini|provider|fetch failed|ENOTFOUND|401|403|API key|read` → **0 hits**. Broad 24h logs → only 3 seed lines. The owner's ~06:56 UTC read is inside this window; if it had reached this pod it would be logged. It is not.

⇒ The owner's working Smart Reader Live (UI renders, credits work, collections seeded, then `/read` fails) is served by a **DIFFERENT deployment** — the one that actually owns `targetmediagrp.com`. This agent's tools all resolve to `visa-booking-5`'s job id and **cannot inspect that other deployment**.

## This also corrects — not contradicts — the earlier RCA
The earlier "Live = Sep-17 build 440f08be without Smart Reader" was **correct for THIS deployment** (visa-booking-5). The mistake was the implicit assumption that this deployment is the one the owner uses. It is not. Both RCAs describe visa-booking-5 accurately; the owner has been on a separate deployment the whole time.

## Answers to the 8 required proofs — from the scope available
1. **Did POST /api/smart-reader/read reach the backend?** Not on this deployment — **0** read log entries in 24h. Cannot be observed from this scope for the owner's actual environment.
2. **Actual HTTP status returned?** Unknown from this scope (request never touched this pod).
3. **Real exception / stack trace?** None present in this deployment's logs (only seed lines). Cannot retrieve the owner's server error from here.
4. **Runtime env `EMERGENT_LLM_KEY` (printenv of the serving process)?** Not obtainable: (a) no tool execs into a live pod; (b) `list_secrets` for THIS run shows `EMERGENT_LLM_KEY`, `SMART_READER_PROVIDER`, `SMART_READER_MODEL` all ABSENT — but that is THIS deployment's secret set, not the owner's serving deployment. The owner's env must be checked on the deployment that serves targetmediagrp.com.
5. **`node_modules/emergentintegrations` and `node_modules/pdfjs-dist` in the running container?** Cannot verify — no live-pod exec; the eph sandbox reflects THIS run's source (wrong deployment) and was additionally in an infra outage. Must be checked in the owner's actual serving container.
6. **`smart_reader_usage` docs (status/file_type/error) for last 48h?** `collection_not_found` in this deployment's DB — the collection does not exist here. The owner's `failed_no_charge` usage docs live in the other deployment's DB.
7. **Corrected build/commit statement:** For THIS deployment there was **no newer deploy** and **no date misread** — active run `440f08be`, image `us-central1-docker.pkg.dev/emergent-default/customers-app/visa-booking-5:440f08be...`, `created_at 2026-09-17T21:14:39Z`, `completed_at 2026-09-17T21:19:29Z`, `deployment_updated_at 2026-09-17T21:19:29Z`; single pod `...m76v6`, restart_count 0; `recent_runs` shows nothing after it. The feature "demonstrably runs on Live" only because the owner's Live is a different deployment. (BUILD_ID / image digest from inside the container remains unread — eph outage — but is moot: it's the wrong deployment for the read failure.)

## Precise failing stage (pdf-extract vs LLM-call vs parsing)
**Cannot be determined from this scope** — the failing request, its logs, and its `smart_reader_usage.error` field are all in the other deployment. Do NOT infer the stage from this deployment's data.

## Required next step (this is a scoping/config issue, not a code fix I can produce here)
Re-run this exact RCA **scoped to the deployment that actually serves `targetmediagrp.com`** (its own X-Deploy-Job-ID / deployment_id — NOT `660386d0…`/visa-booking-5). On that deployment, in a ±3h window around 06:56 UTC:
- Pull `POST /api/smart-reader/read` request logs + the server exception/stack (grep the same terms).
- Read that deployment's `smart_reader_usage` docs (last 48h) — the `status`/`file_type`/`error` fields name the failing stage verbatim.
- Confirm `EMERGENT_LLM_KEY` / `SMART_READER_PROVIDER` / `SMART_READER_MODEL` in that run's secret set, and `emergentintegrations` / `pdfjs-dist` in that image.

Until the investigation is pointed at the correct deployment, the read-pipeline failure cannot be root-caused — the evidence does not exist in `visa-booking-5`.
